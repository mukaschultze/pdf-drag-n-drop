import { PdfElement } from "./pdf-element";

export interface PdfList extends PdfElement {

    // public build(payload?: ReportPayload) {
    //     return this.getBuildedChildren(payload).pipe(
    //         map((children) => ({ ol: children })),
    //     );
    // }

}
